# Compute Node Agent Requirements

# BPF/eBPF for system metrics collection
# NOTE: BCC must be installed as a system package, not via pip:
#   Ubuntu/Debian: sudo apt-get install python3-bpfcc bpfcc-tools
#   CentOS/RHEL:   sudo yum install python3-bcc bcc-tools
#   Fedora:        sudo dnf install python3-bcc bcc-tools

# gRPC for streaming metrics to server
grpcio>=1.60.0,<1.62.0
grpcio-tools>=1.60.0,<1.62.0
protobuf>=4.21.6,<5.0.0

# etcd client for configuration and heartbeat (use etcd3-py fork for protobuf 4.x compatibility)
etcd3-py>=0.1.6

# GPU monitoring (NVIDIA)
pynvml>=11.4.1

# Standard library enhancements
typing-extensions>=4.0.0
